
<script src="/scripts/common/header_main.js"></script>
<script src="/scripts/common/mainRight.js"></script>


<% if (typeof page.scripts !== 'undefined' && page.scripts instanceof Array) { %>
  <% page.scripts.forEach(function(jsPath) { %>
    <script src="<%= jsPath %>"></script>
  <% }) %>
<% } %>

